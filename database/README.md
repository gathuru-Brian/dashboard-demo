# Database

This folder is reserved for database scripts, migrations, and schema artifacts.

## Suggested contents

- `migrations/`
- `init.sql`
- `seed_data.sql`
- `ERD.png`

The backend uses `backend/app/database.py` to manage the SQLite database connection for this prototype.
